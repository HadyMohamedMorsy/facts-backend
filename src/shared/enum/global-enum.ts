export enum Gender {
  MALE = "male",
  FEMALE = "female",
}
export enum AuthType {
  Bearer,
  None,
}

export enum ROLES {
  ADMIN = "admin",
  USER = "user",
  EMPLOYEE = "employee",
  EMPLOYER = "employer",
}
